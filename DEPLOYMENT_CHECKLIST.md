# 📋 部署前檢查清單

在部署到 GitHub Pages 前，請完成以下步驟：

## ✅ 部署前準備

### 1. 確認本地測試正常
- [ ] `npm install` - 依賴已安裝
- [ ] `npm run dev` - 開發服務器正常運行
- [ ] `npm run build` - 構建成功，無錯誤
- [ ] 在瀏覽器中測試功能（搜尋、篩選、單位轉換）

### 2. 驗證單位轉換邏輯
- [ ] 運行 `node src/utils/unitConverter.test.js` - 所有測試通過
- [ ] 在 UI 中手動測試單位轉換（例如 ml → cup）

### 3. 配置 GitHub Pages 參數

#### 方法 A：部署到 GitHub Pages 子路徑（推薦新手）
```bash
# 1. 編輯 package.json
# 改為: "homepage": "https://你的用戶名.github.io/Recipe-Repo"

# 2. 編輯 vite.config.js
# 改為: base: '/Recipe-Repo/',
```

#### 方法 B：部署到自定義域名
```bash
# 1. 編輯 package.json
# 改為: "homepage": "https://你的域名.com"

# 2. 編輯 vite.config.js
# 改為: base: '/',
```

### 4. 提交代碼到 Git
```bash
git add .
git commit -m "Initialize Recipe Repo project"
git push origin main
```

### 5. 啟用 GitHub Pages（如果尚未啟用）
1. 進入 GitHub 倉庫
2. Settings → Pages
3. Source 選擇 "Deploy from a branch"
4. 選擇 "gh-pages" 分支

### 6. 部署
有三種方式選擇其一：

#### 選項 1：使用 GitHub Actions（完全自動）
```bash
# 只需推送代碼，GitHub Actions 會自動部署
git push origin main
# 等待 Actions 完成（通常 1-2 分鐘）
```

#### 選項 2：手動使用 gh-pages 命令
```bash
npm run deploy
# 會自動構建並推送到 gh-pages 分支
```

#### 選項 3：手動構建並上傳
```bash
npm run build
# 將 dist/ 文件夾內容上傳到託管平台
```

## 🔍 驗證部署成功

### 檢查 1：GitHub Pages 配置
- [ ] 進入倉庫 Settings → Pages
- [ ] 確認 Source 指向 "gh-pages" 分支
- [ ] 複製顯示的 GitHub Pages URL

### 檢查 2：訪問網站
- [ ] 在瀏覽器中訪問你的 GitHub Pages URL
- [ ] 應該看到 Recipe Repo 標題和 3 個示例食譜
- [ ] 搜尋功能正常
- [ ] 標籤篩選正常
- [ ] 單位轉換正常

### 檢查 3：開發者工具
打開瀏覽器開發者工具（F12）檢查：
- [ ] Network 標籤 - 所有資源都加載成功（沒有 404 錯誤）
- [ ] Console 標籤 - 沒有 JavaScript 錯誤
- [ ] 所有 CSS 正確加載和應用

### 檢查 4：性能
- [ ] 頁面加載速度（應該很快，< 2 秒）
- [ ] 單位轉換響應時間（應該即時，< 100ms）

## 🐛 故障排除

### 問題 1：頁面顯示 404
**原因**：base 路徑不正確
```bash
# 檢查 vite.config.js 的 base 配置
# 應該與 package.json 的 homepage 路徑一致
```

### 問題 2：樣式不顯示或錯亂
**原因**：CSS 路徑問題
```bash
# 清除瀏覽器緩存或使用無痕模式
# 檢查 Network 標籤確認 CSS 文件已加載
```

### 問題 3：GitHub Actions 部署失敗
**排查步驟**：
1. 檢查 GitHub Actions 運行日誌（Actions 標籤）
2. 確認 gh-pages 分支存在
3. 檢查倉庫權限

### 問題 4：單位轉換不工作
```bash
# 運行測試驗證邏輯
node src/utils/unitConverter.test.js

# 檢查瀏覽器 Console 是否有錯誤
```

## 📱 跨設備測試

部署後，建議在多個設備上測試：

### 桌面
- [ ] Chrome
- [ ] Firefox
- [ ] Safari（如果有 Mac）

### 手機
- [ ] iOS Safari
- [ ] Android Chrome

### 測試項目
- [ ] 搜尋功能
- [ ] 標籤篩選
- [ ] 單位轉換選擇器
- [ ] 響應式布局
- [ ] 按鈕點擊反應

## 📊 性能測試（可選）

使用 Google Lighthouse 測試（Chrome 開發者工具）：
- [ ] 性能評分 > 90
- [ ] 無障礙評分 > 90
- [ ] 最佳實踐評分 > 90

## 🎉 完成清單

- [ ] 本地測試通過
- [ ] 配置參數已修改
- [ ] 代碼已推送到 GitHub
- [ ] GitHub Pages 已配置
- [ ] 部署完成
- [ ] 網站可訪問
- [ ] 功能正常
- [ ] 跨設備測試通過

## 📚 有用的命令

```bash
# 本地開發
npm run dev

# 構建
npm run build

# 預覽構建結果
npm run preview

# 部署到 GitHub Pages（如果配置了 gh-pages）
npm run deploy

# 測試單位轉換
node src/utils/unitConverter.test.js

# 查看 Git 狀態
git status

# 提交更改
git add .
git commit -m "Your message"
git push origin main
```

## 🔗 相關文檔

- [README.md](README.md) - 完整項目文檔
- [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) - 詳細部署指南
- [QUICKSTART.md](QUICKSTART.md) - 快速開始
- [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) - 項目總結

---

準備好後，祝你部署順利！🚀
